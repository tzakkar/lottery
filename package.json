{
  "name": "lottery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd product && npm ci",
    "build": "cd product && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "node-xlsx": "^0.12.1"
  }
}
